/**
 * Empty stub module for web-only dependencies that Metro needs to resolve
 * but are never invoked in native code.
 *
 * Example: @dotlottie/react-player is imported by lottie-react-native's
 * index.web.js, but never executed in native builds. This stub allows
 * Metro to complete module resolution without crashing.
 */

export default undefined;
