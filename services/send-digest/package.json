{
  "name": "@wfl/send-digest",
  "version": "1.0.0",
  "description": "Lambda function to send daily digest notifications",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "jest"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/client-sns": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x",
    "date-fns-tz": "^2.x"
  },
  "devDependencies": {
    "@types/node": "^20.x",
    "typescript": "^5.x"
  }
}
