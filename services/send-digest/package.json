{
  "name": "@wfl/send-digest",
  "version": "1.0.0",
  "description": "Lambda function to send daily digest notifications",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "node --test --experimental-test-module-mocks --import tsx/esm src/**/*.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.x",
    "@aws-sdk/client-sns": "^3.x",
    "@aws-sdk/lib-dynamodb": "^3.x",
    "date-fns-tz": "^2.x"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "typescript": "^5.7.0",
    "tsx": "^4.7.0"
  }
}
